# Server
PORT=4000

# Auth service (used to verify JWT tokens)
AUTH_SERVICE_URL=https://your-auth-service.com
JWT_SECRET=your-secret-key-change-this

# Database
DB_HOST=127.0.0.1   # use 'mysql' if running inside Docker
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=kelsey_payroll
